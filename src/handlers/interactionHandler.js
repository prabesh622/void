const { errorEmbed } = require('../utils/embeds');

/** Handle button, select menu, and modal interactions */
function handleInteraction(interaction, client) {
  // Buttons are handled in individual event files for modularity
  // This handler provides the routing framework
}

module.exports = { handleInteraction };
